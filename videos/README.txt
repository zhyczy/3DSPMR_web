Drop demo.mp4 here (referenced by index.html as videos/demo.mp4).

For a YouTube embed instead, replace the <video> block in index.html with:

  <iframe src="https://www.youtube.com/embed/VIDEO_ID"
          class="w-full h-full" allowfullscreen></iframe>
